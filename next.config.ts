import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Turbopack is now the default in Next.js 16
  // swcMinify has been removed - use turbopack instead
};

export default nextConfig;