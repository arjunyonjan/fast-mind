// eyeStrain.ts
export function calcEyeStrain(hour: number, isSleeping: boolean, hoursSlept: number): number { const hoursAwake = isSleeping ? 0 : (hour < 6 ? hour + 18 : hour - 6); const base = Math.min(10, Math.floor(hoursAwake * 0.8)); const recovery = isSleeping ? Math.min(10, hoursSlept * 1.5) : 0; return Math.max(0, base - recovery); }