// brainScore.ts
export function calcBrainHealth(inputs: any): number { let health = 100; const { accumulationPercent, glymphaticPercent, lifestyle, mood, circadian, isAwake, sunlight, social, learning, breathwork, eyeStrain } = inputs; if (accumulationPercent > 60) health -= 25; else if (accumulationPercent > 30) health -= 10; if (glymphaticPercent < 30) health -= 20; else if (glymphaticPercent < 60) health -= 10; if (lifestyle.includes("Less Sleep")) health -= 15; if (lifestyle.includes("Processed / Junk Food")) health -= 12; if (lifestyle.includes("Alcohol")) health -= 15; if (lifestyle.includes("Smoking")) health -= 20; if (lifestyle.includes("No Exercise")) health -= 10; if (mood === "depressed") health -= 20; if (mood === "angry") health -= 15; if (mood === "anxious") health -= 10; if (circadian === "NIGHT" && isAwake) health -= 35; if (circadian === "NIGHT" && !isAwake) health += 10; if (sunlight) health += 8; if (social) health += 10; if (learning) health += 7; if (breathwork) health += 6; health -= eyeStrain * 3; return Math.max(0, Math.min(100, health)); }